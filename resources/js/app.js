import './bootstrap';
import Alpine from 'alpinejs';
import Carousel from './carousel';
import DarkModeToggler from './darkModeToggler';

window.Alpine = Alpine;
Alpine.start();

window.Carousel = Carousel;

window.DarkModeToggler = DarkModeToggler;
